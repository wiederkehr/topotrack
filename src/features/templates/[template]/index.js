const name = "";

const variables = [];

const presets = [];

const Render = ({ activity, activityData, variables, format, size }) => {
  return null;
};

export default {
  name,
  variables,
  presets,
  Render,
};
